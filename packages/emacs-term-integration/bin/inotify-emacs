#!/bin/bash

set -eu

function usage() {
    echo "Usage: $(basename "${0}") file" >&2
    exit 1
}

test -z "${1}" && usage
test ! -f "${1}" && usage
test "${#}" -gt 1 && usage

FILE="${1}"

inotify-cmd "${FILE}" emacs-term-cmd 'term-inotify' "${FILE}"
