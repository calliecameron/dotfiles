#!/bin/bash

set -eu

sudo bash -l -c "nix-env --delete-generations '${DOTFILES_NIX_GC_AFTER}'"
nix-env --delete-generations --profile "${DOTFILES_NIX_PROFILE}" "${DOTFILES_NIX_GC_AFTER}"

nix-store --gc
