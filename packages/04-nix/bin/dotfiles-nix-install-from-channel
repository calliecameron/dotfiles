#!/bin/bash

set -eu

function usage() {
    echo "Usage: $(basename "${0}") channel attr [attrs...]" >&2
    exit 1
}

test -z "${1:-}" && usage
test -z "${2:-}" && usage

nix-env --install --profile "${DOTFILES_NIX_PROFILE}" --file "<${1}>" --attr "${@:2}"
