# -*- shell-script -*-

pip install 'ipython<7' jupyter 'jupyter-console<6' snakeviz jedi rope importmagic flake8 autopep8 yapf pylint pep8 rlipython

if python --version 2>&1 | grep 'Python 3\.' >/dev/null; then
    pip install mypy
fi
