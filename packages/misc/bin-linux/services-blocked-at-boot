#!/bin/bash
# This may not be entirely accurate if the override file has different contents

find /etc/init -name '*.override' 2>/dev/null | while read -r line; do
    basename "${line}" '.override'
done
