#!/bin/bash
# Convert a decimal number to hex

set -eu

function usage() {
    echo "Usage: $(basename "${0}") number" >&2
    exit 1
}

test -z "${1:-}" && usage
test "${#}" -gt 1 && usage

printf "%x\n" "${1}"
