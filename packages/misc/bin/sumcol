#!/bin/bash
# Sum a column of numbers

set -eu

function usage() {
    echo "Usage: $(basename "${0}") file" >&2
    exit 1
}

test -z "${1:-}" && usage
test "${#}" -gt 1 && usage

awk "{sum+=\$${1}}; END {print sum}"
