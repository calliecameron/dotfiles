#!/bin/bash
# Convert a hex number to decimal

set -eu

function usage() {
    echo "Usage: $(basename "${0}") number" >&2
    exit 1
}

test -z "${1:-}" && usage
test "${#}" -gt 1 && usage

printf "%d\n" "0x${1}"
