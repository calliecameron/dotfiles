#!/bin/bash

set -eu

if [ "$(gsettings get org.cinnamon.desktop.interface gtk-theme)" = "'${DOTFILES_CINNAMON_THEME_DARK}'" ]; then
    dotfiles-set-cinnamon-theme light
else
    dotfiles-set-cinnamon-theme dark
fi
